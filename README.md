# Тестовые задания

Набор из четырёх тестовых [заданий](doc/tasks.pdf) для разработчика солвера.

**Проект находится в разработке.**

## Сборка

Проект написан в Vsual Studio 2022 под Windows, конфигурации Debug и Release настроены только для платформы x64.

Также есть возможность собрать проект с помощью CMake под Linux или Windows, в директории с файлом `CMakeLists.txt`:

    mkdir build
    cd build
    cmake ..
    cmake --build .

### Тестовое задание #1

Файлы, относящиеся к этому заданию:

    vxor.h
    vxor.cpp
    
Побайтовое исключающее или для массива большого размера (от 500'000 до 999'999). Реализовано на основе SSE2 и AVX2 (векторизации с помощью SIMD-инструкций), а именно XOR двух 128-битных или 256-битных векторов. Демонстрируется функциями `runTest1()` и `runTest1Ex()`, последняя содержит референтную реализацию базового алгоритма, с помощью которой проверяется оптимизированное решение (сверяются байты).

### Тестовое задание #2

Файлы, относящиеся к этому заданию:

    peval.h
    
Это простая реализация метода Горнера. Демонстрируется функцией `runTest2()`.

### Тестовое задание #3

Файлы, относящиеся к этому заданию:

    rational.h
    
Реализация представления рационального числа в виде дроби, для нахождения НОД используется алгоритм Евклида. Демонстрируется функцией `runTest3()`.

### Тестовое задание #4

Файлы, относящиеся к этому заданию:

    mmul.h
    mmul.cpp

Умножение квадратной матрицы на вектор-столбец больших размеров (от 1'000 до 1'999). Демонстрируется функцией `runTest4Ex()`. Внешний цикл (цикл по строкам матрицы) распараллеливается с помощью OpenMP (в конфигурации Debug нет флагов для поддержки OpenMP, т.к. это бессмыслено). Внутренний цикл (умножение строки матрицы на вектор) обрабатывается с помощью AVX. Когда OpenMP параллелит внешний цикл, каждый поток обрабатывает одну строку матрицы. Внутри каждого потока выполняется SIMD-умножение строки матрицы на вектор с использованием AVX, то есть поэлементное умножение двух векторов (по 4 числа с плавающей точкой двойной точности в каждом векторе) и два горизонтальных (попарных) сложения получившегося вектора самого с собой, но c переставленными частями (пополам) при первом сложении. Таким образом, оба метода (OpenMP и AVX) работают вместе для параллельной и векторизованной обработки данных.

Также как и в задании #1 есть одноимённая функция в пространстве имён `ReferenceAlgorithms`, т.е. референтный вариант алгоритма, который неэффективен, но надёжен. С помощью этих функций проверяются результаты. Версии функций `A`/`U`, как и в первом задании, отличаюся наличием/отсутствием выравнивания входных данных (кратность адреса 16ти/32ум), первая быстрее.

***Сложение изменено: при первом горизонтальном сложении вектор складывается сам с собой, а второе сложение уже вертикальное и с перестановкой частей полученного при первом сложении вектора (пополам), используемого в качестве второго слагаемого. Вертикальное сложение гораздо эффективнее горизонтального.***

Latency and Throughput (_mm256_hadd_pd )
| Architecture       | Latency | Throughput (CPI) |
| ------------------ | ------: | ---------------: |
| Alderlake          |       5 |                2 |
| Icelake Intel Core |       6 |                2 |
| Icelake Xeon       |       6 |                2 |
| Sapphire Rapids    |       5 |                2 |
| **Skylake**        |       7 |                2 |

Latency and Throughput (_mm256_add_pd)
| Architecture       | Latency | Throughput (CPI) |
| ------------------ | ------: | ---------------: |
| Alderlake          |       2 |              0.5 |
| Icelake Intel Core |       4 |              0.5 |
| Icelake Xeon       |       4 |              0.5 |
| Sapphire Rapids    |       2 |              0.5 |
| **Skylake**        |       4 |              0.5 |

***После выполнения относительно небольшого количества инструкций `_mm256_mul_pd` ([VEX.256.66.0F.WIG 59 /r VMULPD ymm1, ymm2, ymm3/m256](https://www.felixcloutier.com/x86/mulpd)), то есть перемножения групп по 4 числа с плавающей точкой двойной точности 300 раз, набюлюдается устойчивое расхождение с результатом референтного варианта алгоритма в 7-8 знаке после точки как с выравниванием входных данных (матрицы и вектора по 32-байтной границе), так и без. К сожалению, в AVX512F мой старый-добрый `Intel® Core™ i5-8250U Processor` не умеет играть.***

***Выполнен переход к динамическому выделению памяти с помощью интринсиков (intrinsics) для удобства тестирования.***
